// NOTE: macros is code that writes other code (REALLY COMPLICATED STUFF TO WRITE)
// really nice stuff to use
// Some examples given below:

pub fn macros() {
    // NOTE: this is a macro
    let v1 = vec![1, 3, 2];

    // NOTE: again println!() is also a macro
    println!("{:?}", v1);
}
